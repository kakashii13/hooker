
export enum ComposeStatus {
    NONE = 0,
    LOADING = 1,
    SUCCESS = 2,
    ERROR = -1,
  }
  
  export enum DragImageStates {
    ERROR = -1,
    NONE = 0,
    DRAG_OVER = 1,
    UPLOADING = 2,
    COMPLETE = 3,
  }